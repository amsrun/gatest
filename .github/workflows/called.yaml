name: Reusable Print Environment Variable

on:
  workflow_call:
  workflow_dispatch:

jobs:
  my-dynamic-env-job:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: |
          echo "${{ vars.VAR }}"